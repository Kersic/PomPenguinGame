const mocha = require ("mocha");
const assert = require("chai").assert;

describe('test_pingvina',function(){
   it('Test pingvina',function(){
      let restult = 'Heloo';
      assert.equal(restult,'Heloo');
   });
});
